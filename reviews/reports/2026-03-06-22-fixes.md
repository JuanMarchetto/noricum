All done. Here's the summary:

---

## Summary of Changes

### Fixed (code-level)

| # | Issue | File | Change |
|---|-------|------|--------|
| P1 | Cache lacks TTL invalidation | `crates/noricum-core/src/orchestrator.rs:212-280` | Added 7-day TTL: `is_fresh()` checks file mtime, expired entries are removed on access |
| P2 | REST API missing per-IP rate limiting | `crates/noricum-cli/src/api.rs:28-55, 98-130` | Added `IpRateLimiter` with sliding-window counter (default 60 RPM), `rate_limit_middleware` extracts IP from `x-forwarded-for` or peer addr. Configurable via `NORICUM_RATE_LIMIT_RPM` env var |
| P2 | (cont.) | `crates/noricum-cli/src/main.rs:249-254` | Wired rate limiter into `AppState` construction |
| P3 | `LlmClient::run_prompt` duplicates match arms | `crates/noricum-agents/src/providers.rs:53-73` | Extracted `run_prompt_on<C: CompletionClient>()` generic helper function; match arms now delegate to it |
| P4 | No edge case tests for empty/binary/malformed C | `crates/noricum-core/src/orchestrator.rs` (tests) | Added 5 tests: `test_migrate_empty_c_file`, `test_migrate_binary_content`, `test_migrate_preprocessor_only`, `test_migrate_malformed_c`, `test_migrate_massive_single_line` |
| P5 | Successful migrations not added to RAG store | `crates/noricum-core/src/orchestrator.rs:849-871` | After cache put, auto-adds validated C→Rust pair as a `MigrationPattern` (truncated to 500 chars, tagged with function name) |
| P6 | Docker image lacks resource limit guidance | `Dockerfile:19-23` | Added OCI labels with recommended memory (2g), CPUs (2), pids-limit (256) and usage comment |

### Skipped (not code-level or not actionable)

| # | Issue | Reason |
|---|-------|--------|
| P5 | Capture actual API token counts | rig-rs 0.31 doesn't expose usage metadata (noted in `lib.rs:29`); requires upstream support |
| P6 | Commit `release.yml` | File already exists at `.github/workflows/release.yml` — needs `git add`, not a code fix |
| P8 | Create PR template | Already exists and is tracked: `.github/pull_request_template.md` |
| P8 | Record demo GIF | External action (not a code fix) |

### Validation

- `cargo check --workspace` — **pass**
- `cargo test --workspace` — **336 tests pass, 0 failures**
- `cargo clippy --workspace -- -D warnings` — **0 warnings**
