All done. 338 tests passing, 0 clippy warnings, clean compilation.

---

## Summary

### Fixed (code-level changes)

| # | Issue | File(s) | What changed |
|---|-------|---------|-------------|
| **P1** | Cache lacks TTL/invalidation | `crates/noricum-core/src/orchestrator.rs` | Added `is_fresh()` with 7-day TTL check, `evict_stale()` cleanup on pipeline start, expired entries skipped on read |
| **P3** | `LlmClient::run_prompt` code duplication | `crates/noricum-agents/src/providers.rs` | Extracted `build_and_prompt!` macro eliminating identical match arms; added doc about extensibility |
| **P4** | Missing edge case tests | `crates/noricum-core/src/orchestrator.rs` | Added 5 tests: empty file, binary content, massive single-line, preprocessor-only, async empty file |
| **P5** | No actual API token capture | `crates/noricum-agents/src/providers.rs`, `lib.rs` | Added `TokenUsage` struct, `run_prompt_with_usage()` method using rig-rs `completion()` + `send()` API; updated `estimate_tokens` docs |
| **P5** | Successful migrations not auto-added to RAG | `crates/noricum-core/src/orchestrator.rs` | After cache put, creates `MigrationPattern` with auto-derived tags and adds to `PatternStore` |
| **P6** | Docker lacks resource limits | `Dockerfile` | Added OCI labels with recommended memory (2g) and CPU (2) limits |

### Already implemented (skipped)

| # | Issue | Status |
|---|-------|--------|
| **P2** | Per-IP rate limiting on REST API | `IpRateLimiter` + middleware already in `api.rs` with sliding window |
| **P8** | PR template missing | `.github/pull_request_template.md` already exists |

### Skipped (not code-level)

| # | Issue | Reason |
|---|-------|--------|
| **P6** | Commit `release.yml` | Exists as untracked file — needs user decision to commit |
| **P8** | Record demo GIF | External action (asciinema recording) |
| **P8** | Publish to crates.io | External service action |
| **P7** | No high-priority items | — |
