All done. Here's the summary:

---

## Summary of Changes

### Fixed (13 issues across 10 files)

| # | Issue | Perspective | File(s) Changed |
|---|-------|-------------|-----------------|
| 1 | **Remove dead `interactive.rs` module** | P1, P3 | `crates/noricum-cli/src/main.rs` (removed `#[allow(dead_code)] mod interactive`), `crates/noricum-cli/Cargo.toml` (removed `similar`, `chrono` deps) |
| 2 | **Replace `unwrap()` in API middleware** | P2, P3 | `crates/noricum-cli/src/api.rs:79-91` → replaced 5 `unwrap()` with `expect("valid header value literal")` |
| 3 | **Replace `unsafe` env var manipulation** | P3 | `crates/noricum-agents/src/providers.rs:146-163` → rewrote test to avoid `unsafe` `set_var`/`remove_var` |
| 4 | **Add size validation to all MCP tools** | P2 | `crates/noricum-mcp/src/server.rs` → added `MAX_MCP_SOURCE_SIZE` checks to `check_compilation`, `get_idiomatic_score`, `repair` |
| 5 | **Add Haiku tier for Easy tasks** | P5 | `crates/noricum-agents/src/providers.rs:96` → Easy tasks now use `CLAUDE_3_5_HAIKU` instead of Sonnet |
| 6 | **Clean up Ollama stubs** | P1, P5 | `crates/noricum-core/src/orchestrator.rs` (4 sites) → replaced silent fallbacks with explicit `CoreError::UnsupportedProvider`, `crates/noricum-core/src/lib.rs` (added error variant) |
| 7 | **Pin Rust version in Dockerfile** | P6 | `Dockerfile:2` → `rust:bookworm` → `rust:1.94-bookworm` |
| 8 | **Add input size limits** | P6 | `crates/noricum-core/src/orchestrator.rs` → added `MAX_C_SOURCE_SIZE` (5MB) check in both `migrate_file_sync` and `migrate_file` |
| 9 | **Add LLM rate limiting** | P2 | `crates/noricum-core/src/orchestrator.rs` → added `max_llm_calls` field (default 20), `max_tokens_budget` default (500K), enforced in `check_budget()`. `crates/noricum-cli/src/main.rs` → added `--max-llm-calls` CLI flag |
| 10 | **Add REST API curl examples** | P7 | `README.md` → new "REST API" section with 6 curl examples |
| 11 | **Add end-to-end LLM parsing test** | P4 | `tests/mock_llm_pipeline.rs` → added `test_full_mock_llm_pipeline_e2e` (parse → extract → compile → score → validate) |
| 12 | **Enforce coverage threshold** | P4 | `.github/workflows/ci.yml` → added coverage threshold check (50% minimum) |
| 13 | **Restore `noricum.toml`** | P1 | `noricum.toml` → restored with updated values (Haiku tier, budget limits, source size) |

### Skipped (non-code / external actions)

| Issue | Perspective | Reason |
|-------|-------------|--------|
| Commit `CODE_OF_CONDUCT.md` | P8 Blocking | Git action, not code change |
| Create demo GIF/video | P8 High | External media production |
| Set up release automation / git tags | P6, P8 High | Process/infra, not code |
| Implement actual Ollama provider | P5 High | Major feature, not a fix |
| Improve token estimation with API metadata | P5 High | Requires real API integration |
| Shell completions via `clap_complete` | P7 Nice-to-have | Skipped per instructions |

### Verification

- `cargo check --workspace` — **pass**
- `cargo test --workspace` — **299 tests pass** (12 new)
- `cargo clippy --workspace -- -D warnings` — **0 warnings**
